import React from 'react'  // 导入react

export default class NavLeft extends React.Component {
    render() {
        return (
            <div>
                NavLeft
            </div>
        );
    }
}