import React from 'react'  // 导入react

export default class Header extends React.Component {
    render() {
        return (
            <div>
                Header
            </div>
        );
    }
}