import React from 'react'  // 导入react

export default class Footer extends React.Component {
    render() {
        return (
            <div>
                Footer
            </div>
        );
    }
}